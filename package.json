{
  "name": "grace-hello-mcp",
  "version": "1.0.0",
  "description": "A very simple remote MCP server (Streamable HTTP) that advertises a custom icon.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "make-icon": "python3 make_icon.py"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2"
  }
}
